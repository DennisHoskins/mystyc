import { Icon } from 'lucide-react';
import { crab } from '@lucide/lab';

export default function Cancer({ className } : { className?: string }) {
  return (
    <Icon iconNode={crab} className={className} />
  );
};


